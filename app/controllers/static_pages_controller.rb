# Did I create the proj with --database-postgresql + the other command?
# Is my controller named correctly?
class StaticPagesController < ApplicationController
    # did I define the root correctly?
    def root
    end
end